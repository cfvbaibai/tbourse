logging:
  level:
    root: INFO
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql:
              BasicBinder: TRACE
    cfvbaibai:
      tbourse:
        account:
          storage:
            TransactionMapper: DEBUG

spring:
  datasource:
    test:
      driver-class-name: org.h2.Driver
      url: jdbc:h2:mem:ypsilon;DB_CLOSE_ON_EXIT=TRUE
      username: sa
      password: sa
  jpa:
    hibernate:
      ddl-auto: none
